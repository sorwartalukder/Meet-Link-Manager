import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sorwartalukder.meetlinkmanager',
  appName: 'Meet Link Manager',
  webDir: 'dist'
};

export default config;
